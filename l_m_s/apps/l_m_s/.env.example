# Copy this file to .env and fill in your values (optional).
# The Flutter app currently uses dart-define or code defaults for Sanity.
# Use .env if you add flutter_dotenv and load these at runtime.

# Sanity CMS (optional override; defaults are in main.dart and sanity_client_helper.dart)
# SANITY_PROJECT_ID=w18438cu
# SANITY_DATASET=production

# Optional: token for reading draft content (never commit the real token)
# SANITY_API_TOKEN=sk...
